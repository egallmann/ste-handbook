# Architecture IR

This chapter will explain the Architecture IR as the canonical compiled model of architectural intent. Normative definitions and constraints belong in `ste-spec`; this text is explanatory only.
