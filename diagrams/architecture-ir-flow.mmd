%% End-to-end flow from intent artifacts through compilation into Architecture IR,
%% then to projections and kernel validation. Replace placeholder with IR pipeline detail.

flowchart LR
  placeholder[Architecture IR flow diagram placeholder]
